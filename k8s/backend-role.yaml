apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: rag-backend-role

rules:
  - apiGroups: [""]

    resources:
      - pods
      - services
      - configmaps
      - secrets

    verbs:
      - get
      - list